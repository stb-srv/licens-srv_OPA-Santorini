// Kein separates DB-Setup nötig — SQLite-Datenbank wird beim ersten
// "npm start" automatisch unter DB_PATH (Standard: ./data/licens.db) angelegt.
console.log('ℹ️  Bei SQLite ist kein separates Setup nötig. Starte: npm start');
